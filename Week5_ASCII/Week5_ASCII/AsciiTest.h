#pragma once

class AsciiTest {
private:

public:
	void doSomething();
	void convertDigitsSmallLetters();
	void capitalizeSmallLetters(); // convert a-z to A-Z
	void smallLettersCapital(); // convert A-Z to a-z
};
